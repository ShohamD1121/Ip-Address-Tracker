export type DashboardStateProps = {
  dashboardData: any;
  setDashboardData: any;
};

export type DashBoradDataTypes = {
  ipAddress: string;
  location: string;
  timezone: string;
  isp: string;
  latitude : string,
  longitude : string
};
