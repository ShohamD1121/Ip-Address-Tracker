export type DashboardStateProps = {
  setDashboardData: any;
  dashboardData: any;
};

export type DashBoradDataTypes = {
  ipAddress: string;
  location: string;
  timezone: string;
  isp: string;
  latitude: string;
  longitude: string;
};
